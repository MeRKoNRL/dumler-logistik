'use client';

import React, { useState } from 'react';
import { sendPasswordResetEmail } from 'firebase/auth';
import { auth } from '../firebase';

;

export default function ResetpasswordPage() {
  return <div>ResetpasswordPage content</div>;
}
